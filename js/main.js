const { createApp } = Vue

createApp({
    data() {
        return {
            linksName: [
                'Home', 'Pages', 'Courses', 'Features', 'Blog', 'Shop',
            ]
        }
    },


}).mount('#app');